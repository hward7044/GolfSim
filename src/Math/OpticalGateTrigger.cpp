#include "Math/OpticalGateTrigger.hpp"

bool OpticalGateTrigger::checkOpticalGate(const cv::Mat& currentFrame) {
    // Stub: perform cv::absdiff on gateROI sub-region and threshold
    return false;
}
